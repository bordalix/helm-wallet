import { Mnemonic, Satoshis } from './types'

const listUnspents = (mnemonic: Mnemonic) => {}

export const balance = (mnemonic: Mnemonic) => {}

export const selectUtxos = (mnemonic: Mnemonic, amount: Satoshis) => {}
